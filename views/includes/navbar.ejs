<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-lg">
  <div class="container-fluid">
    <a class="navbar-brand fs-4" href="/">JeriMarketplace</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- Menu di sebelah kiri -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">  
        <li class="nav-item">
          <a class="nav-link active fs-4" aria-current="page" href="/">Home</a>
        </li>
        <% if (isLoggedIn === "true") { %>
          <li class="nav-item">
            <a class="nav-link fs-4" href="/add-product">Add Product</a>
          </li>
        <% } %>
      </ul>
      <!-- Tombol di sebelah kanan -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if (isLoggedIn === "true") { %>
          <li class="nav-item">
            <a class="nav-link fs-4" href="/logout">Logout</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link fs-4" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link fs-4" href="/sign-up">Sign Up</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
